package main

import "fmt"

const p string = "death & taxes"
const w = `Death of auto tune`
const myage = 18

func main() {

	const q = 42
	const alpha = `wolverine`

	fmt.Println("p - ", p)
	fmt.Println("q - ", q)
	fmt.Println("w - ", w)
	fmt.Println("myage", myage)
	fmt.Println(`the alpha is -`, alpha)
}

// a CONSTANT is a simple unchanging value
